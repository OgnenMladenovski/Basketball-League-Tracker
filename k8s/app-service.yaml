apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: basketball-league-tracker
spec:
  type: ClusterIP
  selector:
    service: app
  ports:
    - name: http
      port: 80
      targetPort: 8080