apiVersion: v1
kind: Service
metadata:
  name: adminer
  namespace: basketball-league-tracker
spec:
  type: ClusterIP
  selector:
    service: adminer
  ports:
    - name: http
      port: 80
      targetPort: 8080