spring.application.name=basketball-league-tracker
server.port=${SERVER_PORT:8080}
server.servlet.session.tracking-modes=cookie

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:basketball}
spring.datasource.username=${DB_USER:basketball}
spring.datasource.password=${DB_PASSWORD:basketball}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

spring.thymeleaf.cache=false

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true

league.seed-data=${SEED_DATA:true}
